# 🔥 RAM US Auto Parts - Топовый интернет-магазин автозапчастей

Современный WebApp магазин автозапчастей для Telegram с премиальным дизайном в мужском стиле.

## ✨ Основные возможности

### 🛍️ Для покупателей:
- **Каталог** - 13,000+ товаров с быстрым поиском и фильтрацией
- **Поиск** - по названию, артикулу, производителю
- **Корзина** - добавление товаров, изменение количества
- **Рассрочка 0%** - на выбранные товары (12 месяцев)
- **Оформление заказа** - простая форма с доставкой по всей России
- **Категории** - иерархическое дерево с подкатегориями
- **Карточка товара** - полное описание, характеристики, фото

### 🎨 Дизайн:
- Темная тема в стиле RAM (черный, серебристый, красный)
- Glassmorphism эффекты
- Skeleton-лоадеры для плавной загрузки
- Анимированный Splash Screen с логотипом
- Адаптивная верстка для всех устройств
- Bottom Navigation как в топовых приложениях

### ⚙️ Для администратора:
- **Админ-панель** - управление товарами, категориями, заказами
- **Курс валют** - автоматическое получение курса USD/RUB из API ЦБ РФ
- **Уведомления** - в Telegram при новом заказе
- **Импорт** - массовая загрузка товаров из Excel
- **Статистика** - по заказам и продажам

## 🚀 Технологии

### Frontend:
- **Next.js 14** (App Router, Turbopack)
- **React 19**
- **TypeScript**
- **Tailwind CSS v4**
- **Shadcn/UI** - компоненты
- **Zustand** - state management (корзина)
- **Framer Motion** - анимации

### Backend:
- **FastAPI** - современный Python фреймворк
- **PostgreSQL** - база данных
- **SQLAlchemy** (async) - ORM
- **Aiogram** - Telegram Bot
- **SQLAdmin** - админ-панель
- **httpx** - для API курса валют

## 📦 Установка и запуск

### 1. Клонировать репозиторий
```bash
cd ram-us
```

### 2. Backend

```bash
cd backend

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
cat > .env << EOF
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ramus_db
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
WEBAPP_URL=http://localhost:3000
ADMIN_CHAT_ID=YOUR_TELEGRAM_CHAT_ID
EOF

# Создать базу данных
createdb ramus_db

# Запустить сервер
uvicorn app.main:app --reload --port 8000
```

Админ-панель: http://localhost:8000/admin

### 3. Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Создать .env.local
echo "NEXT_PUBLIC_API_URL=http://localhost:8000" > .env.local

# Запустить dev-сервер
npm run dev
```

WebApp: http://localhost:3000

### 4. Telegram Bot

```bash
cd backend
source venv/bin/activate
python -m app.bot
```

## 📊 Импорт товаров

### Из Excel файла:
```bash
cd backend
source venv/bin/activate
python -m app.import_price_xls
```

Формат файла:
- **Наименование производителя** - бренд
- **Номер детали** - артикул
- **Наименование детали** - название
- **Наличие Шт** - количество на складе
- **Цена реализации Шт** - цена в рублях

### Создание категорий:
```bash
python -m app.seed_categories
```

## 🎯 API Endpoints

### Товары:
- `GET /products/` - список товаров (с фильтрами: category_id, search, skip, limit)
- `GET /products/{id}` - товар по ID
- `POST /products/` - создать товар

### Категории:
- `GET /categories/tree` - дерево категорий

### Заказы:
- `POST /orders/` - создать заказ

### Валюта:
- `GET /currency/usd-rate` - текущий курс USD/RUB

## 🔧 Структура проекта

```
ram-us/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI приложение
│   │   ├── models.py            # SQLAlchemy модели
│   │   ├── schemas.py           # Pydantic схемы
│   │   ├── database.py          # Подключение к БД
│   │   ├── crud.py              # CRUD операции
│   │   ├── bot.py               # Telegram бот
│   │   ├── currency.py          # Курс валют
│   │   ├── seed_categories.py   # Наполнение категорий
│   │   └── import_price_xls.py  # Импорт из Excel
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── app/
│   │   ├── page.tsx             # Главная страница
│   │   ├── layout.tsx           # Layout
│   │   └── globals.css          # Стили
│   ├── components/
│   │   ├── header.tsx           # Шапка с поиском
│   │   ├── bottom-nav.tsx       # Нижняя навигация
│   │   ├── product-card.tsx     # Карточка товара
│   │   ├── splash-screen.tsx    # Загрузочный экран
│   │   ├── ui/                  # Shadcn/UI компоненты
│   │   └── views/               # Страницы (Catalog, Cart, Profile, etc.)
│   ├── lib/
│   │   └── cart-store.ts        # Zustand store для корзины
│   ├── public/
│   │   └── logo_new.png         # Логотип
│   ├── package.json
│   └── next.config.ts
├── docker-compose.yml
├── прайс.xls                    # Пример прайс-листа
└── README.md
```

## 🎨 Цветовая палитра

- **Background**: `#0a0a0a` (почти черный)
- **Primary (RAM Red)**: `#d32f2f` (спортивный красный)
- **Silver**: `#c0c0c0` (металлический серебристый)
- **Accent**: `#9333ea` (фиолетовый для рассрочки)

## 📝 TODO (опционально)

- [ ] Автопересчет цен по курсу в админке
- [ ] Фильтры в каталоге (цена, производитель, наличие)
- [ ] Загрузка фото товаров через админку
- [ ] Интеграция с ЮКасса/CloudPayments для оплаты
- [ ] Интеграция с СДЭК для доставки
- [ ] WebApp авторизация через Telegram
- [ ] История заказов в профиле
- [ ] Избранное

## 🤝 Поддержка

Если возникли вопросы или нужна помощь:
1. Проверьте, что все сервисы запущены
2. Проверьте логи в терминалах
3. Убедитесь, что порты 3000 и 8000 свободны
4. Проверьте .env файлы

## 📄 Лицензия

Проект создан для RAM US Auto Parts. Все права защищены.

---

**STAY TOP! 🔥**





