# 📋 Инструкция по импорту товаров

## Формат файла

Используйте CSV или Excel файл с следующими колонками:

| Колонка | Описание | Обязательно | Пример |
|---------|----------|-------------|--------|
| `name` | Название товара | ✅ Да | Масляный фильть Mopar |
| `part_number` | Артикул | ✅ Да | 68191349AA |
| `description` | Описание | ❌ Нет | Оригинальный масляный фильтр для RAM 1500 |
| `price_rub` | Цена в рублях | ✅ Да | 1200 |
| `price_usd` | Цена в долларах | ❌ Нет | 15.00 |
| `in_stock` | Количество на складе | ✅ Да | 10 |
| `category_name` | Название категории | ✅ Да | Двигатель |
| `brand` | Бренд | ❌ Нет | Mopar |
| `is_featured` | Показывать на главной | ❌ Нет | TRUE/FALSE |
| `image_url` | URL изображения | ❌ Нет | https://example.com/image.jpg |

## Шаблон

Скачайте шаблон: `import_template.csv`

## Импорт через скрипт

### 1. Подготовьте файл

Сохраните таблицу в CSV или Excel формате.

### 2. Запустите импорт

```bash
cd backend
python import_price_xls.py путь/к/файлу.xlsx
```

или

```bash
python run_import.py
```

## Важные примечания

⚠️ **Категории должны существовать!**
- Если категории нет в базе, создайте её сначала через админку
- Или скрипт создаст категорию автоматически (в зависимости от скрипта)

⚠️ **Дубликаты по артикулу**
- Если товар с таким `part_number` уже существует, он будет обновлён
- Цены и наличие обновятся автоматически

⚠️ **Изображения**
- Укажите полный URL изображения
- Или оставьте пустым (будет placeholder)

## Примеры

### CSV файл

```csv
name,part_number,price_rub,in_stock,category_name
Масляный фильтр,68191349AA,1200,10,Двигатель
Воздушный фильтр,33-2247,3500,5,Двигатель
```

### Excel файл

Откройте `import_template.csv` в Excel, заполните данные и сохраните как `.xlsx`

## Проверка после импорта

1. Откройте админку: `/admin`
2. Перейдите в раздел "Товары"
3. Проверьте что товары импортированы
4. При необходимости отредактируйте через админку

## Поддержка

При возникновении ошибок проверьте:
- Правильность названий колонок
- Заполнение обязательных полей
- Формат цен (число без букв)
- Существование категорий

